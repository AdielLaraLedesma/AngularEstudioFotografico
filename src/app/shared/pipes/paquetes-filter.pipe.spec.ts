import { PaquetesFilterPipe } from './paquetes-filter.pipe';

describe('PaquetesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PaquetesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
